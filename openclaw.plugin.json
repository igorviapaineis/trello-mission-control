{
  "id": "trello-mission-control",
  "name": "Trello Mission Control",
  "description": "Multi-agent task orchestration via Trello — claim, pipeline, archive, digest, skill discovery, card hygiene.",
  "version": "3.0.0",
  "contracts": {
    "skills": ["trello-mission-control"],
    "hooks": ["onSessionStart", "onSessionStop", "onGatewayStart"]
  },
  "activation": {
    "onStartup": true
  },
  "configSchema": {
    "type": "object",
    "required": ["board_id", "agents"],
    "properties": {
      "board_id": {
        "type": "string",
        "description": "Trello board ID where coordination happens"
      },
      "archive_board_id": {
        "type": "string",
        "description": "Trello board ID for archiving completed cards (free-tier hygiene)"
      },
      "templates_list_id": {
        "type": "string",
        "description": "Hidden list containing template cards (used by template command)"
      },
      "lists": {
        "type": "object",
        "description": "Map of list name -> Trello list ID",
        "additionalProperties": { "type": "string" }
      },
      "labels": {
        "type": "object",
        "description": "Map of label name -> Trello label ID",
        "additionalProperties": { "type": "string" }
      },
      "pipeline": {
        "type": "array",
        "items": { "type": "string" },
        "description": "Ordered list of list names representing the pipeline stages"
      },
      "agents": {
        "type": "object",
        "additionalProperties": {
          "type": "object",
          "properties": {
            "role": {
              "type": "string",
              "enum": ["orchestrator", "executor"]
            },
            "list_id": {
              "type": "string",
              "description": "Trello list ID this agent picks cards from"
            },
            "label_id": {
              "type": "string",
              "description": "Trello label ID for claim-<agent>"
            }
          },
          "required": ["role"]
        }
      }
    }
  }
}
